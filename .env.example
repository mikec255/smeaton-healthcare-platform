# Azure App Service Production Environment Variables
NODE_ENV=production

# Database Configuration (Azure Database for PostgreSQL)
DATABASE_URL=your-azure-postgres-connection-string

# Azure App Service URLs (replace with your actual Azure app name)
AZURE_FRONTEND_URL=https://your-app-name.azurewebsites.net
FRONTEND_URL=https://your-app-name.azurewebsites.net

# Authentication (CRITICAL: Use strong session secret in production)
SESSION_SECRET=your-strong-random-production-session-secret-here-minimum-32-chars

# Email Configuration (Brevo/SendGrid)
BREVO_API_KEY=your-brevo-api-key
SENDGRID_API_KEY=your-sendgrid-api-key

# Object Storage (Google Cloud or Azure Blob)
GOOGLE_CLOUD_PROJECT_ID=your-project-id
GOOGLE_CLOUD_KEYFILE_PATH=/path/to/service-account.json

# GitHub Integration
GITHUB_TOKEN=your-github-token

# App Configuration (Azure sets PORT automatically)
VITE_API_URL=https://your-app-name.azurewebsites.net/api